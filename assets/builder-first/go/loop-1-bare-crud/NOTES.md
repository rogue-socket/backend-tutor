# Loop 1 — your notes

Use this file as your scratchpad while running Loop 1. The tutor will read it at the end and use it to pick the emphasis for Loop 2.

Suggested prompts (don't feel obliged to answer all of them):

## What surprised me

[2-3 sentences. The thing that landed harder than expected.]

## Where I got stuck (and what unstuck me)

[E.g., "tried to use http.Handle and was confused about the trailing slash on `/links/` — it's a prefix match. Routing in stdlib is simpler than I thought once that clicked."]

## Numbers I want to remember

[E.g., "default HTTP server has no timeouts — I had to set ReadTimeout / WriteTimeout / IdleTimeout. Defaults are NEVER what you want in prod."]

## Questions for the tutor

[Anything you wanted to ask but didn't want to break flow — drop them here, the tutor reads this at end-of-session.]

## Things I want to come back to

[E.g., "what's the actual cost of `sync.RWMutex` vs `sync.Mutex` for this access pattern?" — these become candidates for the review queue.]
